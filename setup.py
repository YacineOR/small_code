from setuptools import find_packages, setup

setup(
    name="smallcode",
    version='0.1',
    author="yacine",
    description="a small code",
    #packages=find_packages(where="src"),
    #package_dir={"": "src"},
    install_requires=[],
    tests_require=[],
)
